const {{defaultView}}Route = {
    name: 'base.{{defaultView}}',
    url: '{{defaultView}}',
    templateUrl: '/src/js/view/{{defaultView}}/template/{{defaultView}}.html',
    controller: '{{defaultView}}Ctrl',
    controllerAs: '{{defaultView}}'
};

export default {{defaultView}}Route;
